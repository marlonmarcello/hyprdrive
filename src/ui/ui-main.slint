export component AppWindow inherits Window {
    no-frame: true;
    background: transparent;

    Rectangle {
        background: #282a36;
        border-radius: 8px;

        Text {
            text: "hyprdrive panel";
            color: #f8f8f2;
            horizontal-alignment: center;
            vertical-alignment: center;
        }
    }
}
